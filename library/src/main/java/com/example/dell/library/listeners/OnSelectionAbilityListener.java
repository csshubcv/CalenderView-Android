package com.example.dell.library.listeners;

/**
 * This interface is used to inform DatePicker that user selected any days
 */

public interface OnSelectionAbilityListener {
    void onChange(boolean enabled);
}
